# 环境配置

# 数据库连接 (PostgreSQL)
DATABASE_URL=postgresql://postgres:password@localhost:5432/register_manager

# 开发环境使用 SQLite (可选)
# DATABASE_URL=file:./dev.db

# 服务器配置
PORT=3000
HOST=localhost

# 认证密钥 (生产环境请更换)
AUTH_SECRET=your-super-secret-key-change-in-production

# 前端 URL (用于 CORS)
FRONTEND_URL=http://localhost:5173
