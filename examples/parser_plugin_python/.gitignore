# Python plugin specific (local to this plugin)
# Most Python ignores are in root .gitignore

# Build output (plugin specific)
/dist/

# Virtual environment (if created locally in this directory)
/.venv/

# uv lock file (plugin specific dependencies)
/uv.lock
