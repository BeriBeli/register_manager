/**
 * Application constants - centralized configuration.
 * These are used throughout the codebase instead of hardcoded values.
 */

/** Default reset type for field reset values */
export const DEFAULT_RESET_TYPE_REF = "HARD";

/** Default memory map name when creating a new project */
export const DEFAULT_MEMORY_MAP_NAME = "default_map";

/** Default memory map display name */
export const DEFAULT_MEMORY_MAP_DISPLAY_NAME = "Default Memory Map";

/** Default address unit bits for memory maps */
export const DEFAULT_ADDRESS_UNIT_BITS = 8;
